<odoo>
  <data>
    <!-- supplier action window -->
    <record id="supplier_action" model="ir.actions.act_window">
      <field name="name">List Supplier</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">supplier</field>
      <field name="view_mode">tree,form</field>
      <field name="help" type="html">
        <p class="oe_view_nocontent_create">
          <!-- Add Text Here -->
        </p><p>
          <!-- More details about what a user can do with this object will be OK --> 
        </p>
      </field>
    </record>
    <!-- This Menu Item must have a parent and an action -->
    <menuitem
      id="supplier_menu_act"
      name="Supplier"
      parent="material_master_menu_act"
      action="supplier_action"
      sequence="21"
    />
  </data>
</odoo>